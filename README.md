# A custom drag and drop interface

# Dragger: 
a render props wrapper for items that you want to drag.

# DraggerContainer: 
a render props wrapper for a dropzone

# stateReducer: 
a redux store that maintains the drag state